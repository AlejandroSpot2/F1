# Data Folder

Put cleaned datasets for the final charts in this folder.

Suggested convention:

- one file per visualization when charts use different data transforms
- plain CSV whenever possible
- descriptive filenames such as `constructor_points_2025.csv`

If you are using scripts elsewhere to build these files, document the pipeline in the report or in a separate notes file.
